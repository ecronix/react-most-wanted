let resolve,glob,camelCase;_12c‍.x([["default",()=>_12c‍.o]]);_12c‍.w("path",[["resolve",["resolve"],function(v){resolve=v}]]);_12c‍.w("glob",[["default",["glob"],function(v){glob=v}]]);_12c‍.w("camelcase",[["default",["camelCase"],function(v){camelCase=v}]]);/**  EXPORT ALL FUNCTIONS
 *
 *   Loads all files that have a specific extension
 *   Default extension is .f.js
 *   Exports a cloud function matching the file name
 *
 *   Based on this thread:
 *     https://github.com/firebase/functions-samples/issues/170
 */





const _12c‍anonymous=(folder, exports, extension = '.f.js') => {
  const files = glob.sync(`./**/*${extension}`, {
    cwd: resolve(folder),
    ignore: './node_modules/**',
  })

  for (let f = 0, fl = files.length; f < fl; f++) {
    const file = files[f]
    const functionName = camelCase(
      file
        .split(extension)
        .join('')
        .split('/')
        .join('_')
    ) // Strip off '.f.js'
    if (
      !process.env.FUNCTION_NAME ||
      process.env.FUNCTION_NAME === functionName
    ) {
      const mod = require(resolve(folder, file))
      exports[functionName] = mod.default || mod
    }
  }
};_12c‍.d(_12c‍anonymous);
